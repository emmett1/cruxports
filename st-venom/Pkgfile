# Description:	A simple (suckless) terminal
# URL:		https://github.com/venomlinux/st-venom
# Maintainer:	Emmett1, emmett1 dot 2miligrams at gmail dot com
# Depends on:	xorg-libx11 xorg-libxft xorg-libxrender fontconfig freetype ohsnap

name=st-venom
version=20190601
release=1
_commit=c0e732164593f89d033ff1d00688053c47e140a0
source=(https://github.com/venomlinux/st-venom/archive/$_commit.tar.gz
	config.h)

build() {
	cd $name-$_commit

	cp $SRC/config.h .
	make CC=gcc
	make PREFIX=/usr DESTDIR=$PKG TERMINFO=$PKG/usr/share/terminfo install

	# conflict with ncurses
	rm $PKG/usr/share/terminfo/s/st{,-256color}
}
